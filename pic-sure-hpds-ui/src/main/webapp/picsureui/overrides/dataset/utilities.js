define([],  function(){
    return {
        /* List of overrides for query object formatting. */
        format: {},

        /* List of overrides for query object rendering. Used with format outputs. */
        render: {}
    };
});